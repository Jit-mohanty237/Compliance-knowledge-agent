{
  "name": "latam-compliance-agent",
  "version": "1.0.0",
  "description": "LATAM Compliance Agent Workspace",
  "scripts": {
    "backend": "uvicorn backend.api:app --host 0.0.0.0 --port 8000 --reload",
    "frontend": "npm run dev --prefix frontend",
    "start": "uvicorn backend.api:app --host 0.0.0.0 --port $PORT"
  }
}
